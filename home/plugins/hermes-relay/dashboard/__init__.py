"""Hermes-Relay dashboard plugin (server-side).

Loopback-only FastAPI router + pre-built frontend bundle. Mounted by
hermes-agent at ``/api/plugins/hermes-relay/*`` when this plugin's
``dashboard/manifest.json`` is discovered under ``~/.hermes/plugins/``.
"""
