name: web-apinex
version: 1.0.0
description: "APInex web tools: search + extract (POST /v1/tools/web/*) with auto-fallback to exa/firecrawl, plus the web_research tool (multi-step research with citations). Requires APINEX_API_KEY from https://apinex.bond — free web tools tier."
author: Skappa (custom)
kind: backend
provides_web_providers:
  - apinex
