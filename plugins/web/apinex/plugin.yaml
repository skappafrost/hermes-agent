name: web-apinex
version: 1.0.0
description: "APInex web search and content extraction (POST /v1/tools/web/*). Requires APINEX_API_KEY from https://apinex.bond — free web tools tier. Falls back to exa (search) / firecrawl (extract) on failure."
author: Skappa (custom)
kind: backend
provides_web_providers:
  - apinex
