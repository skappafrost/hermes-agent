name: web-unsearch
version: 0.1.0
description: "UnSearch (unsearch.dev) web search + research (5K calls/month free). DORMANT until UNSEARCH_API_KEY is added — their backend (api.unsearch.dev) returned container_unavailable / key-reject as of 2026-09-15. Requires UNSEARCH_API_KEY — https://unsearch.dev."
author: Skappa (custom)
kind: backend
provides_web_providers:
  - unsearch
